<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_main"
    app:startDestination="@id/mainFragment">
    <fragment
        android:id="@+id/mainFragment"
        android:name="ceo.dog.dogapp.ui.main.MainFragment"
        tools:layout="@layout/main_fragment" >
        <action
            android:id="@+id/action_mainFragment_to_breedDescriptionFragment"
            app:destination="@id/breedDescriptionFragment" />
    </fragment>

    <fragment
        android:id="@+id/breedDescriptionFragment"
        android:name="ceo.dog.dogapp.ui.main.BreedDescriptionFragment"
        tools:layout="@layout/main_fragment" />
</navigation>